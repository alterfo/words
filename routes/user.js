(function() {
  "use strict";
  var express, mongoose, passport, router;

  express = require("express");

  router = express.Router();

  mongoose = require("mongoose");

  passport = require("passport");

}).call(this);
