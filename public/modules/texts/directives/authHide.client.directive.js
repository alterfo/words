// Generated by CoffeeScript 1.10.0
(function() {
  angular.module('texts').directive('authHide', [
    'AuthService', function(AuthService) {
      return {
        restrict: 'A',
        link: function(scope, element, attrs) {
          scope.user = AuthService.getUser();
          return scope.$watch(scope.user, function(value, oldValue) {
            if (scope.user) {
              return element.addClass('ng-hide');
            } else {
              return element.removeClass('ng-hide');
            }
          });
        }
      };
    }
  ]).directive('authShow', [
    'AuthService', function(AuthService) {
      return {
        restrict: 'A',
        link: function(scope, element, attrs) {
          scope.user = AuthService.getUser();
          return scope.$watch(scope.user, function(value, oldValue) {
            if (scope.user) {
              return element.removeClass('ng-hide');
            } else {
              return element.addClass('ng-hide');
            }
          });
        }
      };
    }
  ]);

}).call(this);
