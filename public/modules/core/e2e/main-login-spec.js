// Generated by CoffeeScript 1.9.3
(function() {
  describe('500 words login', function() {
    beforeEach(function() {
      return browser.get('http://localhost:3000');
    });
    return it('should be able to login', function() {
      var form, password, username;
      $('.nav [href="/#!/signin"]').click();
      form = $('form.signin');
      username = form.element(By.model('credentials.username'));
      password = form.element(By.model('credentials.password'));
      username.sendKeys('demo');
      password.sendKeys('demodemo');
      form.submit();
      expect(element(By.model('authentication.user.displayName'))).toBe('Demo Demo');
      $('.nav [href="/auth/signout"]').click();
      return expect(element(By.model('authentication.user'))).toBeFalthy();
    });
  });

}).call(this);

//# sourceMappingURL=main-login-spec.js.map
