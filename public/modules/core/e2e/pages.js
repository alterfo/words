// Generated by CoffeeScript 1.10.0
(function() {
  var LoginPage;

  LoginPage = (function() {
    function LoginPage() {
      this.username = element(By.model('credentials.username'));
      this.password = element(By.model('credentials.password'));
      this.login_button = element(By.css('form button[type=submit]'));
      this.login_error = element(By.binding('error'));
    }

    LoginPage.prototype.get = function() {
      browser.get('/#!/signin');
      return browser.getCurrentUrl();
    };

    LoginPage.prototype.setUsername = function(username) {
      this.username.clear();
      this.username.sendKeys(username);
      return this;
    };

    LoginPage.prototype.clearUsername = function() {
      this.username.clear();
      return this;
    };

    LoginPage.prototype.setPassword = function(password) {
      this.password.clear();
      this.password.sendKeys(password);
      return this;
    };

    LoginPage.prototype.clearPassword = function() {
      return this.password.clear();
    };

    LoginPage.prototype.login = function() {
      return this.login_button.click();
    };

    LoginPage.prototype.getErrorMessage = function() {
      return this.login_error.getText();
    };

    return LoginPage;

  })();

}).call(this);
